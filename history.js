//history object is used to revisit the website we go through ,it has three main methods .back(), .forward(), .go()

history.back(); // used to back visit the beneath website we visit
history.forward(); // used to foward visit the upward website we visit
history.go(-1,-2, 1,3);// using the values if +ve moves that much steps forward if they are in -ve then goes backwards