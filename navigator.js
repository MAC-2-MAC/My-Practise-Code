// we run this code in browser console to get the output
console.log(navigator); // navigator is built in property of window object which will present defaultly
console.log(navigator.userAgent); // this method is used to find the browser details like which version

// there are many other things also but mainly used to get the browser details,